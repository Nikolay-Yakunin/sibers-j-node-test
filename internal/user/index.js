const { NewUserRepository } = require("./repository")
const { NewUserService } = require("./service")
const { NewUserHandler } = require("./handler")

module.exports = {
  NewUserRepository,
  NewUserService,
  NewUserHandler
}